# LGP Schemas

No canonical wire schema is defined in Draft 0.1.

This directory will contain protocol schemas only after a binding or canonical representation is accepted through public protocol review.
